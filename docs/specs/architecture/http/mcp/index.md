# HTTP — MCP

- `POST /mcp` — one JSON-RPC request
- `GET /mcp` and `DELETE /mcp` — rejected; `Allow: POST`

Scenarios:

- [protocol.md](./protocol.md) — `HTTP-MCP-PRT-*`
- [tools.md](./tools.md) — `HTTP-MCP-TLS-*`
- [boundary.md](./boundary.md) — `HTTP-MCP-BND-*`
